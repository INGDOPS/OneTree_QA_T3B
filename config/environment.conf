# BOT Automation V3 - configuration
# Credentials are intentionally externalized. Export them before running:
export BOT_USER_GT='jeroroapa'
export BOT_PASS_GT='Store1234A'
export BOT_USER_TRAINEE='jestovhue'
export BOT_PASS_TRAINEE='Store1234A'
export BOT_USER_GD='germera'
export BOT_PASS_GD='Store1234A'
export POS_SCREEN_DIR="${POS_SCREEN_DIR:-$HOME/evidencias_bot/screenshots}"
export POS_VIDEO_DIR="${POS_VIDEO_DIR:-$HOME/evidencias_bot/videos}"
export POS_LOG_DIR="${POS_LOG_DIR:-$HOME/evidencias_bot/logs}"
export POS_DATA_DIR="${POS_DATA_DIR:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../data" && pwd)}"
export POS_IMAGE_DIR="${POS_IMAGE_DIR:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../images" && pwd)}"
export POS_SIKULI_DIR="${POS_SIKULI_DIR:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../sikuli" && pwd)}"
export POS_DISPLAY="${POS_DISPLAY:-:0.0}"
export POS_VIDEO_SIZE="${POS_VIDEO_SIZE:-1920x1080}"
export POS_VIDEO_FPS="${POS_VIDEO_FPS:-15}"
export POS_KEY_DELAY="${POS_KEY_DELAY:-100}"
export POS_SCREENSHOT_DELAY="${POS_SCREENSHOT_DELAY:-1}"
export POS_DEFAULT_WAIT="${POS_DEFAULT_WAIT:-2}"
export POS_LONG_WAIT="${POS_LONG_WAIT:-25}"
export POS_ALIVIO_TIMEOUT="${POS_ALIVIO_TIMEOUT:-3}"
 
